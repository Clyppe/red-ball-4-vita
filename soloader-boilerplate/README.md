# Red Ball 4 so-loader

Vita homebrew that loads Android `libcocos2dcpp.so`. See the parent `README.md` for install and build steps.

Based on [soloader-boilerplate](https://github.com/v-atamanenko/soloader-boilerplate).
